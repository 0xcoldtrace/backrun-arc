# fixture A0 — thiếu flash_provider → refuse
strategy = "backrun"
chain_id = 5042
dry_run = true
allow_live = false
bot_armed = false
live_mode = "off"
pairs_arb_path = "pairs_arb.txt"
min_profit_usdc = 2.0
arb_max_borrow_usdc = 20000
pairs_min_swap_usdc = 200
min_depth_usd = 8000
min_base_fee_gwei = 20
pending_enabled = false
config_reload_sec = 15
